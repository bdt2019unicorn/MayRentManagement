name: management.mayapartment.com

on:
  push:
    branches: [ master ]

  workflow_dispatch:

jobs:
  DeployChangesToServer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to the server 
        uses: genietim/ftp-action@releases/v4
        with:
          host: ftpupload.net	
          user: b17_27686411
          password: hoangthanhbinh1994
          remoteDir: maymanagementtest.22web.org/htdocs